# Placeholder — this file will be encrypted by sops after you set up your age keys.
#
# To initialize:
#   1. Generate your admin age key:
#      age-keygen -o ~/.config/sops/age/keys.txt
#   2. Copy the public key from the output
#   3. Replace the placeholder in .sops.yaml with your real public key
#   4. Encrypt this file:
#      sops -e -i secrets/secrets.yaml
#   5. Now you can edit secrets with:
#      sops secrets/secrets.yaml
#
# Example secrets (add these after encrypting):
#   github_token: ghp_xxxxxxxxxxxx
#   tailscale_auth_key: tskey-auth-xxxxxxxxxxxx
#   catalog_agent_token: your-forge-api-token
#   wifi_password_home: your-wifi-password

# Placeholder values (replace after encrypting with sops)
github_token: REPLACE_ME
tailscale_auth_key: REPLACE_ME
catalog_agent_token: REPLACE_ME
